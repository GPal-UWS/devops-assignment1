events {
	worker_connections 1000;
}

http {
	upstream flask-weight-converter {
		server 127.0.0.1:8080;
	}
	server {
		listen 80;
		server_name localhost;

		location / {
			proxy_pass http://flask-weight-converter;
		}
	}
}